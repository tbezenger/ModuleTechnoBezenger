<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="fr.univtln.tbezenger858.animaux.mesanimaux.MainActivity">

    <Button
        android:id="@+id/mamButton"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:text="+ Mammifere"
        android:onClick="addAnimal"/>

    <Button
        android:id="@+id/OisButton"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:text="+ Oiseau"
        android:onClick="addAnimal"/>

    <ListView
        android:id="@+id/listMammiferes"
        android:layout_width="150dp"
        android:layout_height="match_parent"
        android:layout_below="@id/mamButton"
        android:background="@android:color/darker_gray" />

    <ListView
        android:id="@+id/listOiseaux"
        android:layout_width="150dp"
        android:layout_height="match_parent"
        android:layout_below="@id/OisButton"
        android:layout_alignParentRight="true"
        android:background="@android:color/holo_blue_bright"/>
</RelativeLayout>
